<?php
// TEMPLATE for config.php - real values live on the staging server only.
// Copy this to config.php on the server and fill in real credentials.
// Do NOT commit the real config.php (see .gitignore).
